import 'package:flutter/material.dart';

class FullContentWidget extends StatelessWidget {
  const FullContentWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      
    );
  }
}
